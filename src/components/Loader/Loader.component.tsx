import React, { FC } from 'react';
import { LoadingWrapper, LoadingIcon } from './Loader.style'

const Loader:FC = () => {
    return  <LoadingWrapper>
                <LoadingIcon />
            </LoadingWrapper>
    }

export default Loader;
